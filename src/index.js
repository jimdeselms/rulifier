const { rulify } = require("./rulify")
const { materialize, getTypeof, getKeys, getLength } = require("./methods")

module.exports = {
    rulify,
    materialize,
    getTypeof,
    getKeys,
    getLength,
}
